/* Array Type */
let arr = [1, 2, 3];
let str = ['a', 'b', 'c'];
/* generic type 타입 변수 => 함수 */
let _arr = [1, 2, 3];
// 유니온 타입 
let multi = ['hi', 2, true];
/* Tuple Type */
// 길이가 고정되어 있다.(배열처럼 길이가 동적으로 변하지 않음)
// 자리가 정해져 있다.
let tupleA = [1, 2];
// 다차원 배열
const user = [
    ['a', 20],
    ['b', 21],
    ['c', 22]
];
export {};
